server:
  port: 8085
spring:
  application:
    name: demo
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: ${JDBC_USER}
    password: ${JDBC_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

jwt:
  secret: LavrenykVitalyViktorovich1BIB22031
  expiration: 3600000